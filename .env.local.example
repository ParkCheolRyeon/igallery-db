# Local dev DB target — copy to .env.local and edit if needed.
# Used by `make migrate-up` etc. NEVER point at production.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/app_dp?sslmode=disable
